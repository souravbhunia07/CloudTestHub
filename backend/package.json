{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "build": "tsc",
    "upload-results": "npm run build && node dist/src/storage/uploadResults.js",
    "database:test": "npm run build && node dist/src/database/testDatabase.js",
    "test:cloud": "npm run build && node dist/src/runner/runTests.js",
    "queue:test": "npm run build && node dist/src/queue/testQueue.js",
    "queue:producer": "npm run build && node dist/src/queue/testProducer.js",
    "queue:worker": "npm run build && node dist/src/queue/worker.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.2",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1131.0",
    "@aws-sdk/client-s3": "^3.1131.0",
    "@aws-sdk/client-sqs": "^3.1131.0",
    "@aws-sdk/lib-dynamodb": "^3.1131.0",
    "dotenv": "^17.4.2"
  }
}
